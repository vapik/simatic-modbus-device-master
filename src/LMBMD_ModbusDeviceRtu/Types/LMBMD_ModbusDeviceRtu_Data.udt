TYPE "LMBMD_ModbusDeviceRtu_Data"
VERSION : 0.1
   STRUCT
      address : UInt;   // Modbus RTU station address
      buffer : Array[0..100] of Word;   // Local buffer
      requests : Array[0..9] of "LMBMD_ModbusDeviceRtu_Request";   // Requests
   END_STRUCT;

END_TYPE

